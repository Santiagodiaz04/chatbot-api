# Chatbot API - CTR Bienes Raíces
# Copiar a .env y ajustar valores

# Base de datos (misma que PHP)
DB_HOST=localhost
DB_NAME=ctr_bienes_raices
DB_USER=root
DB_PASS=

# URL base del sitio PHP (sin / final)
# Desarrollo local:
PHP_BASE_URL=http://localhost/public_html
# Producción:
# PHP_BASE_URL=https://ctrbienesraices.com

# Puerto de la API
PORT=8000

# CORS: orígenes permitidos (separados por coma)
# Desarrollo local:
CORS_ORIGINS=http://localhost,http://127.0.0.1,http://localhost/public_html
# Producción (configurar en Railway):
# CORS_ORIGINS=https://ctrbienesraices.com,https://www.ctrbienesraices.com

# IA generativa (opcional, gratis con Google Gemini)
# 1 = activar; 0 = desactivar (respuestas predeterminadas)
LLM_ENABLED=0
# API key gratis: https://aistudio.google.com/apikey
GEMINI_API_KEY=